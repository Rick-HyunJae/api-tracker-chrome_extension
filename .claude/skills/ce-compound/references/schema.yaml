# Documentation schema for learnings written by ce-compound
# Treat this as the canonical frontmatter contract for docs/solutions/.
#
# Project: API-to-MCP Tracker — Chrome MV3 확장 (TypeScript/React/Vite/Vitest)
#          세 실행 컨텍스트(서비스 워커 / 콘텐츠 스크립트 / UI)로 나뉜다.
#
# The schema has two tracks based on problem_type:
#   Bug track  — problem_type is a defect or failure (build_error, test_failure, etc.)
#   Knowledge track — problem_type is guidance or practice (best_practice, workflow_issue, etc.)
#
# Both tracks share the same required core fields. The tracks differ in which
# additional fields are required vs optional (see track_rules below).

# --- Track classification ---------------------------------------------------
tracks:
  bug:
    description: "Defects, failures, and errors that were diagnosed and fixed"
    problem_types:
      - build_error
      - test_failure
      - runtime_error
      - performance_issue
      - security_issue
      - ui_bug
      - integration_issue
      - logic_error
  knowledge:
    description: "Practices, patterns, conventions, decisions, workflow improvements, and documentation"
    problem_types:
      - best_practice
      - documentation_gap
      - workflow_issue
      - developer_experience
      - architecture_pattern
      - design_pattern
      - tooling_decision
      - convention

# --- Fields required by BOTH tracks -----------------------------------------
required_fields:
  module:
    type: string
    description: "Module or area affected (e.g. src/background/session-manager, src/ui/sidepanel, vite.config.ts)"

  date:
    type: string
    pattern: '^\d{4}-\d{2}-\d{2}$'
    description: "Date documented (YYYY-MM-DD)"

  problem_type:
    type: enum
    values:
      - build_error
      - test_failure
      - runtime_error
      - performance_issue
      - security_issue
      - ui_bug
      - integration_issue
      - logic_error
      - developer_experience
      - workflow_issue
      - best_practice
      - documentation_gap
      - architecture_pattern
      - design_pattern
      - tooling_decision
      - convention
    description: "Primary category — determines track (bug vs knowledge). Prefer the narrowest applicable value; best_practice is the fallback when no narrower knowledge-track value fits."

  component:
    type: enum
    values:
      # --- 실행 컨텍스트 (MV3) ---
      - service_worker       # src/background/ — handleMessage, session-manager, sender, alarms
      - content_script       # src/content/ — content-bridge, widget-host
      - injected_capture     # src/content/injected-capture — 페이지 메인월드 fetch·XHR 패치
      # --- UI ---
      - side_panel           # src/ui/sidepanel/ — Rail, ListView, SummaryBar, DetailView, SettingsView
      - floating_widget      # src/ui/widget/ — FloatingWidget, dock-position (shadow DOM)
      - theme                # src/ui/theme/ — tokens.css, components.css, fonts
      # --- 공유·설정 ---
      - shared_module        # src/shared/ — types, messages, storage, domain-match
      - manifest             # manifest.json — 권한, CSP, web_accessible_resources, 사이드패널 등록
      - build_config         # vite.config.ts, tsconfig.json, @crxjs
      # --- 개발 인프라 ---
      - testing_framework    # Vitest, Testing Library, jsdom, E2E 하네스(agent-browser·CDP)
      - development_workflow # git worktree, 브랜치·통합 규약, 스킬 워크플로
      - documentation
      - tooling              # 위 어디에도 맞지 않는 도구 (fallback — 먼저 위 값들을 검토할 것)
    description: "Primary component involved. 가장 좁게 맞는 값을 고른다. 여러 컨텍스트가 얽히면 문제가 '발생한' 곳을 component로 두고 나머지는 related_components에 적는다."

  severity:
    type: enum
    values:
      - critical
      - high
      - medium
      - low
    description: "Impact severity"

# --- Track-specific rules ----------------------------------------------------
track_rules:
  bug:
    required:
      symptoms:
        type: array[string]
        min_items: 1
        max_items: 5
        description: "Observable symptoms such as errors or broken behavior"
      root_cause:
        type: enum
        values:
          - missing_import
          - missing_index
          - wrong_api
          - scope_issue
          - async_issue
          - async_timing
          - memory_leak
          - config_error
          - logic_error
          - type_error
          - test_isolation
          - missing_validation
          - missing_permission
          - missing_workflow_step
          - inadequate_documentation
          - missing_tooling
          - incomplete_setup
          - duplicate_dependency  # 같은 패키지가 두 인스턴스로 로드 (예: React 이중 인스턴스)
          - environment_gap       # 테스트 런타임이 실제 브라우저와 다름 (예: jsdom 미구현 API)
          - toolchain_gap         # 도구가 특정 검증을 수행하지 않음 (예: esbuild는 타입 검사 생략)
        description: "Fundamental technical cause of the problem"
      resolution_type:
        type: enum
        values:
          - code_fix
          - config_change
          - test_fix
          - dependency_update
          - environment_setup
          - workflow_improvement
          - documentation_update
          - tooling_addition
          - type_fix
        description: "Type of fix applied"

  knowledge:
    optional:
      applies_when:
        type: array[string]
        max_items: 5
        description: "Conditions or situations where this guidance applies"
      symptoms:
        type: array[string]
        max_items: 5
        description: "Observable gaps or friction that prompted this guidance (optional for knowledge track)"
      root_cause:
        type: enum
        values:
          - missing_import
          - missing_index
          - wrong_api
          - scope_issue
          - async_issue
          - async_timing
          - memory_leak
          - config_error
          - logic_error
          - type_error
          - test_isolation
          - missing_validation
          - missing_permission
          - missing_workflow_step
          - inadequate_documentation
          - missing_tooling
          - incomplete_setup
          - duplicate_dependency  # 같은 패키지가 두 인스턴스로 로드 (예: React 이중 인스턴스)
          - environment_gap       # 테스트 런타임이 실제 브라우저와 다름 (예: jsdom 미구현 API)
          - toolchain_gap         # 도구가 특정 검증을 수행하지 않음 (예: esbuild는 타입 검사 생략)
        description: "Underlying cause, if there is a specific one (optional for knowledge track)"
      resolution_type:
        type: enum
        values:
          - code_fix
          - config_change
          - test_fix
          - dependency_update
          - environment_setup
          - workflow_improvement
          - documentation_update
          - tooling_addition
          - type_fix
        description: "Type of change, if applicable (optional for knowledge track)"

# --- Fields optional for BOTH tracks ----------------------------------------
optional_fields:
  related_components:
    type: array[string]
    description: "Other components involved"

  tags:
    type: array[string]
    max_items: 8
    description: "Search keywords, lowercase and hyphen-separated"

# --- Backward compatibility --------------------------------------------------
# Docs created before the track system was introduced may have bug-track
# fields (symptoms, root_cause, resolution_type) on knowledge-type
# problem_types. These are valid legacy docs:
#   - Bug-track fields present on a knowledge-track doc are harmless. Do not
#     strip them during refresh unless the doc is being rewritten for other reasons.
#   - When creating NEW docs, follow the track rules above.

# --- Validation rules --------------------------------------------------------
validation_rules:
  - "Determine track from problem_type using the tracks section above"
  - "All shared required_fields must be present"
  - "Bug-track required fields (symptoms, root_cause, resolution_type) must be present on bug-track docs"
  - "Knowledge-track docs have no additional required fields beyond the shared ones"
  - "Bug-track fields on existing knowledge-track docs are harmless (see backward compatibility note)"
  - "Track-specific optional fields may be included but are not required"
  - "Enum fields must match allowed values exactly"
  - "Array fields must respect min_items/max_items when specified"
  - "date must match YYYY-MM-DD format"
  - "tags should be lowercase and hyphen-separated"
  - "Array-of-strings frontmatter items (symptoms, applies_when, tags, related_components, or any future array field) must be wrapped in double quotes when the value starts with a YAML reserved indicator (`, [, *, &, !, |, >, %, @, ?) or contains the substring `: ` — otherwise strict YAML parsers reject the file"
